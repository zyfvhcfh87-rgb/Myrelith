# Shared title renderer integration

Integrated the reviewed title rendering checkpoint
`9bddfc5248c8b8af26e7e45150272c2d3306af17`, whose production source remains
`54581222b3c46208efea47f71a0d865f52778bc9`, onto accepted milestone integration
`267586c1762343f8eb7323edd7be5d55cd714dc9`.

The merge has no product conflict or manual product correction. It combines the
shared expanded-title renderer and canonical title-effect eligibility with the
accepted tracking controller and schema24 caption metadata/admission. Timeline
schema24 and project format8 remain unchanged. The architecture additions retain
the existing resource and dependency rules. The unified animation workspace,
tracking Inspector and later caption/title authoring are separate unfinished gates.

## Reviewed browser evidence

The exact issue200 checkpoint passed all six muted Chromium flows in19.7seconds,
with no retry. The parent independently inspected all five saved matrices:
396 legacy rows,234 animated rows,18 actual transferred render-bridge rows,
54 compact-mask rows and18 explicit-fallback rows. All2772 pixel comparisons
were exact; the2754 comparisons with recorded line facts also matched. Requested
and observed production canvas settings and all applicable ownership flags passed.
The original HTML baseline/Upgrade controls remain in the suite. The prior failed
mixed-canvas run and complete1008-sample diagnostic are retained, not relabeled.

All nine saved screenshots were inspected. The actual status flow shows missing
font refusal and explicit persisted fallback at1280x720 and720x800. The narrow
view qualifies title/status readability; surrounding shared editor controls remain
dense/clipped, so it does not establish whole-workspace responsive usability.
Algorithm-only ending screenshots show the launcher or plugin recovery screen,
not an editor-ready user-flow claim.

Errors and pageErrors are empty in all six observation records. The animation
proof records six Chromium Canvas2D repeated-readback performance advisories at
the test-only pixel capture, corresponding to its six13-frame raw finite exports.
The destination deliberately uses production settings; changing those settings
to silence the advice would change the reference. Other five flows, including
the actual UI through final screenshots, are warning-free. This is not a blanket
zero-warning console claim.

Native ownership records cover20 runner/server/worker/browser identities. All
exited, no forced termination or observer exception occurred, and fresh process
and port checks confirmed cleanup. Before/after fingerprint
`sha256:83de7ef8716b855b5c6e0251cc10c41ca2af2254da2363f34f4f204fb88d670f`
matches; all778 archived baseline source blobs and18 diagnostic fixture payload
hashes remain intact. Raw artifacts are under
`/private/tmp/issue200-g2-corrected-9bddfc5-03ba2617`.

## Combined-source checks

The merged source passed23 focused files /445 Vitest tests plus17 runner checks,
production build/typecheck, lint and staged diff hygiene. The focused matrix
includes title planning, cache/effect contracts, main/worker/export/UI wiring,
caption legacy presets/persistence/admission/reentrancy and the current schema
fixture. Logs are `/private/tmp/milestone9-title-rendering-integration-tests.log`,
`/private/tmp/milestone9-title-rendering-integration-build.log`, and
`/private/tmp/milestone9-title-rendering-integration-lint.log`. The existing Vite
chunk advisory remains.

The browser result above belongs to the exact issue checkpoint. This merge does
not claim a new combined browser run, encoded-file equivalence, full-suite,
dependency-audit, performance or whole-issue completion. Final mixed-feature
browser/encoded export/PCM and engineering checks remain required before the
single milestone PR is published and merged.
