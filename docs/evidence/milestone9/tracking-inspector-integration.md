# Mask tracking Inspector integration

Merged reviewed tracking Inspector source
`c0bce34d45400d537b583fb79c75cb95da05ba20` and its browser correction checkpoint
`2b3dbd7281909777c558766279729af21ac89611` onto
`c6fad557f90a36ee8173eaccc4d46792def9e6b7` without conflicts or manual product
changes. The shared branch now combines tracking controls with the accepted
title renderer, schema24 caption admission, and existing four preview owners.

The exact issue checkpoint passed all seven muted Chromium flows in26.8seconds:
the five existing direct-mask/path flows, forward point tracking with actual
Save/Open/relink and decoded export assertions, and backward box tracking with
complete-lane replacement consent and abandoned-preview cleanup. The parent
reviewed the complete native output, unchanged assertion source, all nine saved
screenshots, and the actual7,181-byte downloaded project. That file's SHA-256 is
`fd14ba2be6d2b6c51d6f784d985463a234fe1c344351ec7b98376d3653162f42`;
both position lanes retain18 keys with canonical integer source-time ticks.

All seven final console/error assertions passed. The list reporter and
retain-on-failure trace policy did not persist successful JSON body attachments
or an encoded export artifact. Accordingly this is assertion-based functional
evidence, not an independent audit of raw exported pixels or resource values.
Earlier failed attempts and their diagnostics remain preserved. Native file
handles, complete responsive-workspace usability, measured memory/performance,
repeated export, and whole-issue completion remain open.

The original narrow screenshots still show crowded global controls and clipped
lower panels. The reopened tracking screenshot shows connected relinked media;
the backward-box screenshot exposes the complete replacement disclosure. The
parent independently verified all11 observed native process IDs had exited and
port5198 was clear. The run ended at16:02:05UTC, before the host's16:02:31 sleep;
subsequent sleep delayed review and cleanup inspection, not these assertions.

On the combined source,25 focused files /375 Vitest tests and17 runner checks
passed, along with build/typecheck, lint, and staged diff hygiene. This includes
tracking, Inspector, preview arbitration, file boundaries, animation/title
admission and legacy/schema24 caption cases. The existing Vite chunk advisory
remains. Logs are `/private/tmp/milestone9-tracking-inspector-integration-tests.log`,
`/private/tmp/milestone9-tracking-inspector-integration-build.log`, and
`/private/tmp/milestone9-tracking-inspector-integration-lint.log`.

No new combined browser, full-suite, production audit, encoded pixel/PCM or
performance gate is implied. The accepted resource-authority proposal remains
the next mask implementation slice, followed by its own reviewed validation.
