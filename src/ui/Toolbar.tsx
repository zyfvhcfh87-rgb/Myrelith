/**
 * ui/Toolbar.tsx — Top toolbar. Phase 3.1 placeholder; transport buttons
 * land with playback wiring, Export lands in Phase 5.2.
 * Layering: ui/ imports state/ only (nothing yet).
 */

export default function Toolbar() {
  return (
    <div className="panel-placeholder">
      <strong>WebCut</strong>
      <span className="placeholder-note">toolbar — transport &amp; export arrive in later phases</span>
    </div>
  )
}
