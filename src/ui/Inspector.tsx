/**
 * ui/Inspector.tsx — Selected-clip properties. Phase 3.1 placeholder; real
 * transform/opacity editing lands in Phase 4.3.
 * Layering: ui/ imports state/ only (nothing yet).
 */

export default function Inspector() {
  return (
    <div className="panel-placeholder">
      <span className="placeholder-title">Inspector</span>
      <span className="placeholder-note">clip properties arrive in Phase 4</span>
    </div>
  )
}
