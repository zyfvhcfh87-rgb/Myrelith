/**
 * ui/timeline/Timeline.tsx — Timeline container. Phase 3.1 placeholder;
 * Ruler/Playhead land in 3.2, Track/ClipView in 3.3.
 * Layering: ui/ imports state/ only (nothing yet).
 */

export default function Timeline() {
  return (
    <div className="panel-placeholder">
      <span className="placeholder-title">Timeline</span>
      <span className="placeholder-note">ruler, playhead and tracks arrive in 3.2–3.3</span>
    </div>
  )
}
