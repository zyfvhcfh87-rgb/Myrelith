/**
 * ui/MediaPool.tsx — Imported-asset list. Phase 3.1 placeholder; real
 * import UI + mediaStore wiring follows once Preview (3.4) is in.
 * Layering: ui/ imports state/ only (nothing yet).
 */

export default function MediaPool() {
  return (
    <div className="panel-placeholder">
      <span className="placeholder-title">Media Pool</span>
      <span className="placeholder-note">imported files will list here</span>
    </div>
  )
}
